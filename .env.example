# No client env required — all config served by backend GET /api/config
# Dev: Vite proxies /api to http://localhost:5000 (see vite.config.js)
# Prod: Vercel rewrites /api to backend URL (see vercel.json)
# If you must override API target locally, use:
# VITE_API_BASE_URL=http://localhost:5000/api
